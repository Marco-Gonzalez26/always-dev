---
const { classNames, id } = Astro.props
---

<section
  id={id}
  class={`max-w-3xl w-full mx-auto px-6 ${classNames} scroll-mt-10`}>
  <h2 class='text-2xl font-semibold mb-6 flex gap-x-2 items-center'>
    <slot name='icon' />
    <slot name='title' />
  </h2>
  <slot />
</section>
